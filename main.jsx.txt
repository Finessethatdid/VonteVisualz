import React from "react";
import ReactDOM from "react-dom/client";
import VonteVisualz from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VonteVisualz />
  </React.StrictMode>
);